# echoDMS-gRPC
This is the gRPC implementation of echoDMS. It is a separate project from the main echoDMS project, and it is used to implement the gRPC services for echoDMS. The gRPC services are used to communicate between the different components of echoDMS, such as the API Gateway, the Document Service, and the Search Service.

## Architecture

## FastAPI as a Gateway

